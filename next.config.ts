/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/shikharmurty.com",
  output: "export",  // <=== enables static exports
  reactStrictMode: true,
};

module.exports = nextConfig;
